package com.example.ole.oleandroid.controller;

import android.support.v7.app.AppCompatActivity;
import android.widget.ListView;

import com.example.ole.oleandroid.model.PublicLeague;

import java.util.ArrayList;

public class PrivateLeaguesList extends AppCompatActivity {
    //Can we just call the PublicLeagueList methods? Because we want the same stuff for that.
    // Just the list of leagues available which is EPL
}

